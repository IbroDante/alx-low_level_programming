#include <stdio.h>

/**
 * main - Prints.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 59; i < 61; i++)
	{
		putchar(i);
	}
	for (i = 86; i < 222; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
